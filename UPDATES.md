# UPDATES

## 2026-03-27
- `PLAN.md` の内容を確認
- 実装計画 (`implementation_plan.md`) の作成
- タスクリスト (`task.md`) の作成
- サーバーサイド、コントロール画面、ディスプレイ画面の全実装を完了
- `display.html` を画像サイズ 1920x1080 のレイヤースタッキング構造に修正
- 指示された座標に基づいてテキスト位置とスタイル（30px, bold）を適用
- 操作GUI（`control.html`）にホットキーのキーリスニング設定機能を実装
- キーリスニング中に Escape でキャンセル、Delete/Backspace で割り当て解除できる機能を追加
- 普通の数字キーとテンキー（Numpad）を区別して登録・操作できる機能を追加
- キーバインドとチーム名を `config.json` に保存し、再起動後も保持する機能を実装（カウント等はリセット）

## 2026-04-02
- `control.html` に固定フッターを追加
    - コピーライト表記「©どどど素人 dododo-shirouto 2026」を追加
    - 指定された X アカウントへのリンクを設定
    - デザイン：半透明グラデーション背景、右寄せ、操作を邪魔しないための `pointer-events: none` 設定
- `README.md` を作成
    - システム概要、セットアップ、各画面のURL、デフォルトホットキー、ディレクトリ構成を記述

## 2026-04-03
- スコアボードの入力欄とスコア合計表示で「X」や文字列を入力可能に変更
- 空欄（未入力）にした場合、初期表示の `-` 等と見た目が合致するように `scoreboard.html` の表示を修正
- `server.js` および `control.html` の計算ロジックを文字列許容型にリファクタリング（「X」や空欄を加算する際は一時的に+1などの正常な数値操作へ補正）

### 次のステップ
1. 実運用のテスト（OBS等への組み込みやテスト操作）
